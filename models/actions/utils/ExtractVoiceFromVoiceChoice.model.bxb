action (ExtractVoiceFromVoiceChoice) {
  description (Pulls the voice out of the VoiceChoice structure and returns it. Used when a user clicks on a voice.)
  type (Constructor)
  collect {
    input (voiceChoice) {
      type (VoiceChoice)
      min (Required) max (One)
    }
  }
  output (Voice)
}
