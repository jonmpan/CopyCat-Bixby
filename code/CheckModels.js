module.exports.function = function checkModels (reset, resetInput, resetVoice, input, voice, output) {
  return {
    reset:reset,
    resetInput:resetInput,
    resetVoice:resetVoice,
    input:input,
    voice:voice,
    output:output,
  }
}
